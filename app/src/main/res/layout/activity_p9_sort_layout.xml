<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android" android:layout_width="match_parent"
    android:layout_height="match_parent">


    <include
        app:layout_constraintTop_toTopOf="parent"
        layout="@layout/watch_edit_top_view" />

    <TextView
        android:id="@+id/sortDescTv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textSize="20sp"
        android:textColor="@color/contents_text"
        app:layout_constraintTop_toBottomOf="@+id/editTopLin"
        android:text="长按上下拖动位置"/>

    <com.example.bozhilun.android.b31.sort.SwipeRecyclerView
        android:id="@+id/p9SortRecyView"
        app:layout_constraintTop_toBottomOf="@+id/sortDescTv"
        android:layout_width="match_parent"
        android:layout_marginTop="15dp"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_height="0dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>